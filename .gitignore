# Claude Code per-machine settings
.claude/

# Build outputs are governed per-presentation (see */.gitignore):
# only the published overview build is tracked.

# Python
__pycache__/
*.pyc

# Confidential internal-only doc — never publish to the public Pages site
battlecard-service-tiers-internal.html

# Internal / confidential — never published. Anything under here stays out of the public repo.
/_internal/
